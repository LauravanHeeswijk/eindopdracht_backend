# --- App & uploads ---
spring.application.name=boeken-api
server.port=8081
my.upload_location=image_uploads
my.upload_location2=file_uploads

# =========================================================
# DEV (NU): H2 in-memory ? snel ontwikkelen & testen
# =========================================================
# >>> Laat deze H2-sectie AAN zolang je in dev werkt <<<
spring.datasource.url=jdbc:h2:mem:booksdb;MODE=LEGACY;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# maakt/werkt tabellen bij
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# data.sql na JPA laten draaien (seed authors/categories/users)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# H2 console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# =========================================================
# PROD/LOCAL-PG (LATER): PostgreSQL ? uitkommentarieer dit
# =========================================================
# >>> Als je klaar bent voor Postgres:
# 1) Comment H2-regels hierboven (datasource + dialect + h2 console)
# 2) Haal hieronder de # weg en vul db user/wachtwoord in
#
# spring.datasource.url=jdbc:postgresql://localhost:5432/boeken
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.datasource.driver-class-name=org.postgresql.Driver
#
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
#
# Meestal wil je data.sql NIET automatisch in prod:
# spring.sql.init.mode=never
# spring.jpa.defer-datasource-initialization=false

# Overige
spring.jpa.open-in-view=true
logging.level.org.springframework=INFO
